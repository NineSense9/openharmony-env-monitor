# 云端服务（cloud_ecs）部署与公网连通性验收记录 (2026-09-01)

## 1. 部署信息
- **云服务器**: 百度智能云 BCC (`instance-3i9eje3d`)
- **公网 IP**: `180.76.137.117`
- **操作系统**: Ubuntu 22.04 LTS (x86_64)
- **部署路径**: `/opt/openharmony-env-monitor/cloud_ecs`
- **服务单元**: `openharmony-env-monitor.service` (systemd)
- **MQTT Broker**: `mosquitto.service` (端口 1883)
- **REST API 服务**: `uvicorn cloud_ecs.app.main:app` (端口 8000)

## 2. 开放端口与安全组
- **TCP 8000**: FastAPI HTTP 遥测与告警 API（公网开放）
- **TCP 1883**: Mosquitto MQTT 消息代理（公网开放）

## 3. 验收测试结果（100% 通过）

### 3.1 HTTP RESTful API 验收
- `GET /health` -> `200 {"status":"ok"}`
- `POST /api/telemetry` (正常数据采集入库) -> `200`
- `POST /api/telemetry` (异常超限触发温/湿/光/气 4 项告警) -> `200`
- `GET /api/telemetry/latest?device_id=rk2206-station-01` -> `200`
- `GET /api/alerts?device_id=rk2206-station-01` -> `200` (成功返回告警列表)
- `POST /api/command` (下发控制指令: `motor on`) -> `200` (状态 `pending`)
- `GET /api/command/pending?device_id=rk2206-station-01` -> `200` (板端成功拉取待执行命令)
- `POST /api/command/1/ack` (板端执行回执确认) -> `200` (状态变为 `done`)
- `GET /api/command/pending` -> `200 None` (待执行队列已清空)

### 3.2 MQTT Broker 实时通信验收
- 客户端连接 `180.76.137.117:1883` 握手成功
- 订阅主题: `space/cabin/telemetry`、`space/cabin/command`
- 发布与接收遥测消息: `{"device_id":"rk2206-station-01","temperature":26.2,"humidity":54.8,"lux":115.0,"gas_ppm":18.5,"seq":1}`
- 发布与接收控制消息: `{"target":"motor","action":"on","timestamp":1788250988}`
- 端到端 pub/sub 消息全部收发无丢失。

### 3.3 Swagger / OpenAPI 在线文档
- 在线交互文档地址: `http://180.76.137.117:8000/docs`
