实验：4.6 lab02_key_lcd
阶段：TDD RED（禁止 K3 额外 pinctrl）
命令：cloud_ecs\.venv\Scripts\python.exe -m pytest device\labs\03_lab02_key_lcd\tests -q
结果：1 failed, 4 passed in 0.15s
退出码：1
失败原因：新增契约断言要求 `src/tx_key.c` 不调用 `PinctrlSet`，而当前 PULL_UP
源码仍包含该调用；失败与本次实物 UART 的 `K3 pinctrl failed ret=1` 一致。

随后删除 `PinctrlSet` 调用及错误分支，保留 `LzGpioInit`、
`LzGpioSetDir(..., LZGPIO_DIR_IN)` 和 `LzGpioGetVal`，本地测试转为 `5 passed`。
