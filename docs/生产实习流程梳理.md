# 生产实习全流程信息包(供大模型上下文使用)

> **本文件用途**:作为完整上下文喂给大模型,使其能回答实习相关问题、辅助撰写校友邦日志/周志、总结报告、答辩材料、代码问题排查等。
> **信息来源**:①《生产实习方案-北京软通动力教育科技有限公司.pdf》(8页,方案原文) ②学院老师群发通知(原文) ③《生产实习总结报告模板(2026版)》(沈阳航空航天大学,已提取全文结构)。
> **可信度标注约定**:
> - 【原文】= 源文件原话,可作为事实依据
> - 【推断/建议】= 由原文合理推衍的操作性建议,非官方要求
> - 【背景知识】= 通用技术常识,帮助理解,具体细节以现场企业老师为准
> - 【待确认】= 现场需向老师核实的项
> **当前日期基准**:2026-08-29(周六)。实习 8-31(周一)开始。

---

## 1. 快速事实卡(最重要的硬信息)

| 项 | 内容 |
|---|---|
| 学校/学院 | 沈阳航空航天大学 计算机学院 |
| 实习性质 | 集中生产实习(专业人才培养环节),为期 2 周 |
| 实习时间 | **2026-08-31(周一) 至 2026-09-11(周五)**,共 10 个工作日 |
| 承训企业 | **华清远见** 和 **软通动力** 两家,同学分组分别参加 |
| 本人所属方向 | **软通动力:太空空间站内部环境监测系统全链路开发实训**(OpenHarmony 南北向全栈) |
| 项目形态 | 端云一体化:鸿蒙 APP(北向)+ RK2206 开发板传感器采集(南向)+ MQTT 云端 |
| 考核项 | 出勤、校友邦 10 天日志 + 2 次周志、项目答辩、实习总结报告 |
| 指导配置 | 校内实习责任教师 + 校外企业指导教师,共同指导 |

## 2. 硬性考核与纪律要求【原文·老师通知】

1. **出勤**:按照实习单位规定的工作要求准时出勤。(具体上下班时间【待确认】,一般对标企业 9:00–18:00 上下)
2. **管理**:积极配合、服从实习单位指导教师的管理。
3. **校友邦 APP**(必须认真如实地写):
   - **10 天实习日志**(每个工作日 1 篇,共 10 篇)
   - **2 次周志**
   - 被反馈不合格的日志,要**及时修改和补交**
4. **问题分流**:
   - 系统/技术问题 → 工作时间联系**实习单位(企业)指导老师**
   - 校友邦日志/周志/实习总结报告问题 → 工作时间联系**校内实习责任老师**
5. 【推断/建议】日志当天写、周志周五写,别拖;写日志时保留当天截图/代码片段,报告和答辩直接复用。

## 3. 项目全景:太空空间站内部环境监测端云一体化系统

### 3.1 项目一句话【原文浓缩】

基于 **OpenHarmony 南北向全栈**,用鸿蒙南向开发板 + 空间站舱内环境传感器(气压、温度、湿度、风速),实现**舱内环境数据采集 → 本地显示 → 自动控制 → Wi-Fi 上云 → 鸿蒙 APP 远程监控 → 阈值告警**的全流程闭环。

### 3.2 系统五大链路【原文】

```
感知层 → 鸿蒙终端层 → 网络层 → 云端服务层 → 鸿蒙APP应用层
(传感器)  (RK2206)    (Wi-Fi)   (MQTT/存储)   (ArkTS/ArkUI)
```

### 3.3 六大核心功能模块【原文】

| # | 模块 | 功能点 |
|---|---|---|
| 1 | 多传感器数据采集 | 实时采集环境数据,滤波、误差校准 |
| 2 | 本地终端控制 | 自动阈值调控、声光告警、SPI 本地屏、低功耗休眠 |
| 3 | Wi-Fi-MQTT 双向通信 | 数据上传、远程指令下发、断网缓存恢复 |
| 4 | 鸿蒙 APP 可视化 | 实时数据卡片、历史曲线、异常标红、告警弹窗 |
| 5 | 远程管控 | 自定义告警阈值、远程启停执行设备、操作日志记录 |
| 6 | 工程拓展 | 设备功耗统计、实验数据导出、系统轻量化优化 |

### 3.4 量化技术指标【原文】

- 传感器采集误差 **< 3%**
- 远程指令响应 **< 500ms**
- 断网缓存支持 **24 小时**历史数据存储

### 3.5 技术栈清单

| 层 | 技术/工具 | 【背景知识】说明 |
|---|---|---|
| 北向 IDE | DevEco Studio + SDK + 模拟器 | 华为鸿蒙应用官方 IDE,类 IntelliJ 形态 |
| 北向语言/UI | ArkTS / ArkUI | TS 超集的声明式 UI 框架,@Component/@State/@Builder 等 |
| 可视化 | ECharts(集成进 APP) | 环境曲线图表,历史数据展示 |
| 通信 | MQTT | 发布/订阅协议;topic、QoS、断线重连、遗嘱消息为常见考点 |
| 南向板卡 | RK2206 开发板 | OpenHarmony 轻量系统级开发板;具体板卡型号/外设【待确认】 |
| 南向语言 | C 语言 | 底层驱动、内核裁剪相关 |
| 总线 | GPIO / I2C / SPI | I2C 接温湿度/气压传感器,SPI 接本地屏为典型接法 |
| 南向编译 | Docker 鸿蒙编译环境 + 烧录工具 | 容器内交叉编译,烧录验证 |
| 云端 | MQTT 服务端/云平台、历史数据存储 | 具体云平台(如华为云 IoT/EMQX/OneNET)【待确认】 |

## 4. 十天逐日详细计划(核心章节)

> 天数、主题、详细内容三条均为【原文】;「当日产出」与「日志素材角度」为【推断/建议】。
> 日期映射:Day1=8/31(一) Day2=9/1(二) Day3=9/2(三) Day4=9/3(四) Day5=9/4(五) Day6=9/7(一) Day7=9/8(二) Day8=9/9(三) Day9=9/10(四) Day10=9/11(五)

### 第一阶段:鸿蒙北向 APP 开发(Day1–Day6)

#### Day1(8/31 周一)鸿蒙北向环境搭建与项目需求分析 —— 北向阶段1
- 【原文】① DevEco Studio、SDK、模拟器部署;② ArkTS 基础与 APP 页面框架搭建;③ 拆解空间站系统需求,输出需求文档
- 【推断/建议】当日产出:可运行 Hello World/页面框架工程、需求文档初稿
- 【日志素材角度】环境安装踩坑(网络/SDK 下载/模拟器启动)、ArkTS 与已学语言(Java/TS/C)对比、需求分析如何拆功能模块

#### Day2(9/1 周二)动态数据与可视化界面开发 —— 北向阶段2
- 【原文】① 实时数据卡片、异常告警 UI 开发;② ECharts 环境曲线图表集成;③ 模拟数据动态刷新调试
- 【推断/建议】当日产出:含实时卡片 + 告警 UI + 曲线图的界面,模拟数据驱动刷新
- 【日志素材角度】ArkUI 状态管理(@State 刷新机制)、Web 组件嵌 ECharts 或原生图表选型、模拟数据设计

#### Day3(9/2 周三)MQTT 云端通信与数据对接 —— 北向阶段3(上)
#### Day4(9/3 周四)MQTT 云端通信与数据对接 —— 北向阶段3(下)
- 【原文】① MQTT 协议、接口封装、权限配置;② 云端环境数据拉取、断网容错逻辑;③ 历史数据可视化模块开发
- 【推断/建议】两日产出:APP 连上 MQTT Broker 并展示真实云端数据、历史曲线模块、断网重连/容错
- 【日志素材角度】MQTT 发布订阅模型理解、topic 设计、权限/证书配置踩坑、断网容错思路(缓存/重连)

#### Day5(9/4 周五)远程控制与多级阈值告警 —— 北向阶段4
- 【原文】① 远程指令下发、操作日志存储;② 自定义温湿度/气压/风速告警阈值;③ 异常弹窗提醒功能实现
- 【推断/建议】当日产出:APP→设备指令链路、阈值设置页、告警弹窗;**当天写第 1 篇周志**(覆盖 Day1–5)
- 【日志素材角度】指令下发闭环(下发→执行→回执→日志)、多级阈值(预警/告警/危险)设计

#### Day6(9/7 周一)北向 APP 优化与阶段验收 —— 北向阶段5
- 【原文】① 代码重构、性能优化、单元测试;② Bug 闭环、APP 打包交付;③ 北向阶段成果自查与提交
- 【推断/建议】当日产出:打包好的 APP(HAP)、阶段自查清单/提交记录
- 【日志素材角度】重构前后对比、发现的 Bug 与修复过程、打包发布流程

### 第二阶段:鸿蒙南向嵌入式开发(Day7–Day9)

#### Day7(9/8 周二)RK2206 南向环境与硬件工程 —— 南向阶段1
- 【原文】① Docker 鸿蒙编译环境搭建;② 硬件原理图研读、IO 接口适配;③ 底层工程编译、烧录验证
- 【推断/建议】当日产出:编译通过的底层工程、烧录成功点亮开发板
- 【日志素材角度】Docker 环境搭建、原理图怎么看、第一次烧录成功的体验;南向与北向开发差异感受

#### Day8(9/9 周三)传感器驱动开发与对照实验 —— 南向阶段2
- 【原文】① 温湿度、气压、风速底层驱动;② 多组传感器精度对照实验;③ 原始数据采集、误差分析校准
- 【推断/建议】当日产出:三类传感器驱动、对照实验数据表、校准方案
- 【日志素材角度】I2C/ADC 读传感器原始值、误差从哪来、怎么校准(对标 <3% 指标)

#### Day9(9/10 周四)本地自动控制、屏幕与低功耗 —— 南向阶段3
- 【原文】① SPI 本地屏幕、阈值自动调控、声光告警;② Wi-Fi 数据上传云端;③ 终端功耗测试、休眠节能方案落地
- 【推断/建议】当日产出:本地屏显示 + 自动调控 + 声光告警、设备真实上云、功耗测试记录
- 【日志素材角度】本地闭环(不依赖云端也能控)、上云联调第一次端到端跑通、休眠策略与功耗数字

### 第三阶段:整合与答辩(Day10)

#### Day10(9/11 周五)全链路联调、文档撰写、项目答辩 —— 整合答辩阶段
- 【原文】① 端云全流程闭环综合测试;② 全套工程、实验、安全、可持续文档撰写;③ 小组演示、答辩评审、求职辅导
- 【推断/建议】当日产出:完整演示(采集→本地→云端→APP→远程控制→告警)、全套文档、答辩 PPT/讲稿;**当天写第 2 篇周志**;实习总结报告当天启动
- 【日志素材角度】联调发现的跨层问题及定位过程、答辩复盘、两周整体收获

## 5. 团队分组与岗位职责【原文】

- 4–6 人一组,模拟企业真实岗位分工(每人岗位【待确认】):

| 岗位 | 职责 | 职业能力要求 |
|---|---|---|
| 鸿蒙嵌入式开发工程师(南向) | RK2206 开发板底层驱动、多传感器采集、本地自动控制、WiFi 云端上传、低功耗逻辑开发 | C 语言、GPIO/I2C/SPI、鸿蒙内核编译、Docker 环境、传感器调试、功耗优化 |
| 鸿蒙应用开发工程师(北向) | ArkTS/ArkUI 监测 APP 开发、MQTT 云端对接、数据可视化、远程控制、多级告警实现 | ArkTS 语法、组件开发、物联网通信、图表可视化、APP 性能调优 |
| 物联网全栈工程师 | 统筹系统整体架构、打通端云双向链路、全流程集成测试、项目文档编制 | 兼具嵌入式与鸿蒙应用开发能力,系统故障全链路排查、项目管控 |
| 物联网测试工程师 | 设计软硬件测试用例,完成传感器、网络、远程控制全场景测试,输出 Bug 整改台账 | 数据分析、日志排查、标准化测试报告撰写 |
| (项目经理/文档工程师,由上述角色兼任或单设) | 进度统筹、文档产出 | 项目管理与文档能力 |

## 6. 实训模式与特色【原文】

- **模式**:①真实项目驱动式学习(唯一贯穿项目,走完需求→设计→编码→测试→部署);②三段分层递进(北向→南向→整合答辩,由软件到硬件、分模块到整体);③小组定岗分工;④每日"理论讲解 + 上机实操"双环节。
- **特色**:①产业双师(软通动力鸿蒙/物联网一线工程师授课,流程标准对标企业真实项目);②国产化航天特色项目(OpenHarmony + 空间站场景,可作工程教育认证特色实践支撑材料);③软硬全栈覆盖(嵌入式驱动 + ArkTS 应用 + MQTT + 可视化 + 自动控制);④技术 + 职业素养一体化(工程伦理、项目管理、答辩)。

## 7. 教学目标(工程教育认证九大能力维度)【原文】

1. 问题分析能力(用专业知识分析解决实际工程问题)
2. 问题研究能力(熟练使用 IT 行业最新应用/开发工具)
3. 法律法规认知(从经济、社会、技术、法律、文化多方面评价软硬件系统)
4. 环境与可持续发展意识(评价工程实践对环境/社会可持续发展的影响)
5. 团队合作意识(多专业角色协同)
6. 自主学习意识(了解行业动态、技术标准、法律法规)
7. 沟通交流能力(答辩与报告撰写)
8. 项目管理能力(软硬件产品开发与项目管理流程、工程管理与经济决策)
9. 计算机工程问题设计能力(多学科环境下设计开发)

> 用途提示:总结报告第三~七章、答辩、日志都可挂靠这些目标维度组织语言。

## 8. 实习总结报告模板(2026版)全文结构【原文·已从 .doc 提取】

### 封面页
- 标题:**沈阳航空航天大学生产实习总结报告**
- 字段:院(系)=计算机学院、专业、班级、学号、姓名、带队教师(**此处填写实习责任教师的姓名**)
- 日期落款:2026 年 9 月 11 日
- 信息页:实习单位、指导教师、实习时间(**2026 年 08 月 31 日 至 2026 年 09 月 11 日**)
- 评语页(不用本人写):企业实习指导教师评价、校内带队教师评价、实习单位盖章、实习成绩

### 正文七章(每章下的小问题即评分要点,务必逐条回应)

1. **一、生产实习单位简介** —— 对企业信息、企业名称、企业文化进行描述(软通动力;可查公开资料,注意与实习内容呼应)
2. **二、实习单位的入职教育培训及自己的收获**
   1. 入职教育的内容
   2. 企业的劳动(工作)纪律、规章制度
   3. 自己作为工程师对公众安全、健康及环境保护应具备哪些社会责任
3. **三、实习项目的内容及意义**
   1. 描述实习项目的内容
   2. 分析此实习项目对社会、健康、安全、法律以及文化的影响;描述项目实施的工程管理和经济决策考虑有哪些
4. **四、实习项目中本人的具体工作**
   1. 自己从事所承担的工作的过程中做了哪些工作及具体工作内容
   2. 介绍自己在项目团队中的角色以及自己对这个角色的理解
5. **五、实习过程中遇到的问题及解决方法步骤以及效果**
   1. 遇到了哪些问题,分别是怎样解决的
   2. 解决问题时参考了哪些相关资料
   3. 问题解决后得到的效果如何
6. **六、项目实施过程中与团队中其他成员之间的交流和协作**
   1. 如何与团队其他成员进行交流和协作
   2. 协作中遇到什么问题,自己如何处理
7. **七、实习工作总结**
   1. 理论知识、动手实践、工程实践能力三方面的收获
   2. 实习答辩过程中的总结和收获
   3. 实习结束后自己下一步的目标

- 【推断/建议】素材映射:第二章←Day1 入职/纪律;第三、四章←3 节项目全景 + 自己岗位各日工作;第五章←每日踩坑(环境搭建、MQTT 权限、驱动误差、联调);第六章←分组协作;第七章←Day10 答辩复盘。报告要在答辩后尽快成稿,期间每天日志内容可以直接攒成报告素材。

## 9. 校友邦日志/周志写作规范【推断/建议】

### 日志(10 篇)建议结构(每天 200–400 字,真实具体)
1. 日期/天数/阶段(如 Day3,北向阶段3)
2. 今日理论知识点(当天上午讲了什么)
3. 今日实操完成情况(做了什么、产出什么,贴关键截图/代码点)
4. 遇到的问题与解决(1–2 个真实小问题)
5. 明日计划 / 心得一句

### 10 篇日程对照
| 篇 | 日期 | 主题关键词 |
|---|---|---|
| 1 | 8/31 | 环境搭建、需求分析、需求文档 |
| 2 | 9/1 | 数据卡片、ECharts、动态刷新 |
| 3 | 9/2 | MQTT 协议、接口封装、云端拉数 |
| 4 | 9/3 | 断网容错、历史数据可视化 |
| 5 | 9/4 | 远程指令、阈值告警、弹窗 |
| 6 | 9/7 | 重构优化、打包、北向验收 |
| 7 | 9/8 | Docker 编译环境、烧录验证 |
| 8 | 9/9 | 传感器驱动、误差校准 |
| 9 | 9/10 | 本地控制、上云、低功耗 |
| 10 | 9/11 | 全链路联调、答辩、文档 |

### 周志(2 篇)建议结构
- **第 1 篇**(9/4 前后写):第 1 周整体 = 北向阶段全回顾;本周目标 vs 实际完成;团队协作情况;下周(南向)计划与担心点。
- **第 2 篇**(9/11 写):第 2 周整体 = 南向 + 联调答辩回顾;两周完整项目闭环总结;能力成长(对照第 7 节九大目标挑 3–4 条);对后续学习/求职的影响。
- 注意:日志不合格要及时修改补交【原文】;周志应与日志内容一致、不冲突。

## 10. 背景技术知识速查【背景知识】

> 均为通用常识,用于快速理解方案;具体型号、平台、版本以现场为准。

- **OpenHarmony**:开源鸿蒙;按设备 RAM 分轻量系统(Keystone/内核级)、小型系统、标准系统。RK2206 这类 MCU 板跑轻量/小型系统,APP 侧是标准系统。
- **南北向**:"北向"= 面向应用开发者(ArkTS/ArkUI APP);"南向"= 面向设备/驱动/内核(板卡、固件、HAL)。
- **ArkTS/ArkUI**:声明式 UI;常用装饰器 @Entry/@Component/@State/@Prop/@Link;页面路由 Router/Navigation。
- **MQTT**:发布/订阅;Broker/Client;核心参数 topic、QoS0/1/2、keepalive;断网容错常用离线缓存 + 重连补传(对应"24h 断网缓存"指标)。
- **I2C/SPI/GPIO**:I2C 两线多从机,常接温湿度(AHT20 类)、气压(BMP280 类)传感器;SPI 高速,常驱动 TFT 屏;GPIO 控蜂鸣器/LED(声光告警)与按键;风速传感器多为脉冲/模拟量输入。具体传感器型号【待确认】。
- **低功耗**:常见手段为休眠/停机模式 + 定时唤醒、外设时钟门控、降低上报频率;考核口径通常是电流均值。
- **数据校准**:多组传感器对照实验 → 误差统计(均值/方差/最大偏差)→ 线性校准/滤波(滑动均值、卡尔曼简化版)。
- **DevEco Studio**:鸿蒙应用 IDE;调试用模拟器或真机;南向烧录一般用板厂工具(如 Burner/擦写工具)或烧录器。

## 11. 风险与注意事项【推断/建议】

1. 日志拖写 → 补交/不合格重写成本高,建议当日 23:00 前完成。
2. 需求文档、实验数据、测试台账等过程文档**当天留档**,Day10 要"全套工程、实验、安全、可持续文档",临时补来不及。
3. Git 仓库 Day1 就初始化(北向 app/、南向 device/、docs/ 三目录),阶段验收与答辩直接引用提交记录。
4. 传感器误差 <3% 和指令响应 <500ms 是验收硬指标,Day8 的校准实验和 Day5/Day9 的联调要专门测这两项并留数据。
5. 答辩 = 小组演示 + 评审,演示链路(采集→本地→云端→APP→远程控制→告警)建议 Day9 晚提前彩排。
6. 【待确认】清单:上下班/考勤方式、分组名单与定岗、板卡与传感器具体型号、云端 MQTT 平台、总结报告提交截止时间与提交方式(纸质/校友邦/教务)。

---

## 12. 开发环境配置与进度日志(南向,截至 2026-08-29)【实际配置记录】

### 12.1 环境包内容识别(老师发的 6 个包,均在 `D:\实习\`)

| 包 | 内容 | 对应方案环节 |
|---|---|---|
| `02 VBox.rar` | VirtualBox-6.1.32 安装程序(108M) | Day7 编译环境宿主 |
| `03 MobaXterm.rar` | MobaXterm Portable v21.1 | SSH/串口连开发板 |
| `04 烧写工具.rar` | RKDevTool(瑞芯微烧写工具)+ 用户手册 PDF | Day7 固件烧录 |
| `05 USB驱动.rar` | DriverAssitant(Rockchip USB 驱动安装器) | 板子 USB 识别 |
| `Ubuntu.part1/2.rar` | 现成 VirtualBox 虚拟机(ubuntu.vmdk 9.6G + 快照 4.7G),解压后约 14.5G | Day7-9 南向 Docker 鸿蒙编译环境 |
| ⚠️ | **包编号从 02 开始,01 号包缺失**;这批里没有 DevEco Studio(北向 Day1 就要用),需找老师确认是否另发【待确认】 | |

### 12.2 已完成步骤(按实际执行顺序,含踩坑修复)

| # | 步骤 | 结果 |
|---|---|---|
| 1 | 解压 4 个小包到 `D:\实习\`(各自带同名目录) | ✅ |
| 2 | 解压 Ubuntu 双分卷(14.5G)到 `D:\实习\Ubuntu\` | ✅ |
| 3 | pnputil 静默安装 Rockchip USB 驱动(绕开 GUI 和中文路径坑) | ✅ 已装入驱动库(oem155.inf) |
| 4 | 静默安装 VirtualBox 6.1.32(`--silent --ignore-reboot`) | ✅ |
| 5 | 注册 VM:`VBoxManage registervm D:\实习\Ubuntu\Ubuntu.vbox`(磁盘全是相对路径,直接成功) | ✅ |
| 6 | 冒烟测试第 1 次:**失败** — VM 用了 USB 2.0 控制器,缺 Extension Pack(VERR_NOT_FOUND) | 已修复(见 #7) |
| 7 | 下载安装 **Extension Pack 6.1.32**(版本必须和主程序一致;文件必须命名为 `Oracle_VM_VirtualBox_Extension_Pack-*.vbox-extpack` 否则报 name mismatch) | ✅ |
| 8 | 冒烟测试第 2 次:**失败** — 网卡2 桥接绑定的是老师电脑的 Realtek 有线网卡,本机不存在 | 已修复(见 #9) |
| 9 | 改绑:`VBoxManage modifyvm Ubuntu --bridgeadapter2 "Intel(R) Wi-Fi 6 AX201 160MHz"`(本机唯一网卡) | ✅ |
| 10 | 冒烟测试第 3 次:headless 启动成功,稳定运行 70 秒后正常关机 | ✅ **环境验证可用** |
| 11 | 桌面白屏崩溃修复:GNOME Shell (GJS/SpiderMonkey JIT) 在 Hyper-V/NEM 架构下触发 SIGILL(非法指令)导致白屏崩溃。已配置 `GJS_DISABLE_JIT=1`、`MOZ_DISABLE_JIT=1`、强制 Xorg、显存升至 128MB、内存升至 4096MB、添加 NAT 端口转发 2222->22 | ✅ **GUI 桌面完全恢复正常，已建快照「桌面崩溃已修复」** |

**VM 当前关键配置**:VM 名 "Ubuntu"(Ubuntu 64-bit), 6 核 CPU / 4096MB 内存 / 128MB 显存; 网卡1=NAT(含端口转发 2222->22), 网卡2=桥接 Intel Wi-Fi; USB 控制器开启(USB 2.0); 快照链:`纯净镜像` → `环境安装完毕` → `桌面崩溃已修复`。
**VM 登录凭据(已验证)**: 用户名 `lzdz` / 密码 `123456` (sudo 密码同为 `123456`)。Windows 侧可通过 `ssh lzdz@127.0.0.1 -p 2222` 直接免桥接连接。

### 12.3 交接状态快照(其他模型接手时以此为准)

- VirtualBox 6.1.32 + Extension Pack 6.1.32 已装; rockusb 驱动已装; VM 登录凭据 `lzdz` / `123456` 已确认; GUI 桌面与 SSH 端口已修复验证通过, 当前停在快照「桌面崩溃已修复」。
- **快捷方式已建在 `D:\实习\`**(2026-08-29):`VirtualBox.lnk`(虚拟机管理界面)、`启动Ubuntu虚拟机.lnk`(双击=后台启动 VM 并弹出窗口)、`MobaXterm.lnk`、`RKDevTool烧写工具.lnk`;工作目录均已设为各程序所在目录。
- 6 个 .rar 原包(约 4.7G)保留在 `D:\实习\` 备份,可删;`安装包\` 为空文件夹(老师预留)。
- 尚未做/待现场确认:① VM 内 Docker 容器名与编译命令;② 板卡 USB 串口芯片驱动(如 CH340,不在环境包内);③ 01 号包/DevEco Studio;④ RKDevTool 镜像文件与烧录地址表(老师会给固件)。

### 12.4 日常操作速查(怎么用)

**① 启动/关闭虚拟机**(三选一)
- 图形界面:打开 VirtualBox → 选中 "Ubuntu" → 点「启动」(首次启动建议用 GUI,能看见登录画面)。
- 命令行后台:`"C:\Program Files\Oracle\VirtualBox\VBoxManage.exe" startvm Ubuntu`
- 关机:在 VM 里正常 shutdown;应急 `VBoxManage.exe controlvm Ubuntu poweroff`(相当于拔电,尽量别用)。
- **环境玩坏了**:VirtualBox → 选中 VM → 「快照」页 → 选中 `环境安装完毕`(或最干净的 `纯净镜像`)→ 「恢复」。

**② 网络说明**
- VM 上网走网卡1(NAT),无需配置。
- 从 Windows 连 VM(SSH/传文件)走网卡2 桥接:进 VM 后 `ip addr` 查 IP,Windows 侧 MobaXterm SSH 到该 IP。
- 若换网络环境后启动报 `Nonexistent host networking interface`,用 `VBoxManage list bridgedifs` 查当前网卡名,再 `modifyvm Ubuntu --bridgeadapter2 "<新网卡名>"`。

**③ MobaXterm 连板子串口(Day7-9)**
1. 板子插 USB → 设备管理器 →「端口(COM 和 LPT)」找新增 COMx(没有则是缺串口驱动,找老师要 CH340 类驱动)。
2. MobaXterm → Session → Serial → 端口选 COMx,Baudrate 115200(常用值,以现场为准),Flow control 选 None → OK。
3. 按板子复位键,窗口里滚出启动日志即连通。

**④ RKDevTool 烧录(Day7)**
1. 板子进入烧录模式(Loader/MaskROM,按键组合以现场教学为准);设备管理器出现 "Rockusb Device" 说明 12.2#3 装的驱动生效。
2. 打开 `D:\实习\04 烧写工具\RKDevTool\RKDevTool.exe`:「升级固件」页烧整包固件,或「下载镜像」页按老师给的地址表分区烧写;加载老师提供的镜像文件后点「执行」。
3. 烧录失败常见原因:没进烧录模式、驱动没装对、USB 线是纯充电线。

**⑤ Docker 编译(Day7,细节现场对齐)**
- 进 VM 开终端,`docker ps -a` 看老师预置的容器 → `docker start <容器名>` + `docker exec -it <容器名> bash` 进入,再按现场教的命令编译(OpenHarmony 轻量系统一般走 `hb set` / `hb build` 流程)。
- VM 与 Windows 互传代码:没配共享文件夹,建议直接用 Git 仓库中转(和"开仓库"动作合并);临时传可用 `scp`(走桥接网络)。

**⑥ 南向整体使用顺序(Day7 现场对齐后)**
启动 Ubuntu VM → 确认 Docker 编译环境 → 板子插 USB 装好串口驱动 → MobaXterm 串口看日志 → RKDevTool 烧录 → 串口确认固件跑起来。

## 13. 开工前准备状态(截至 2026-08-29 傍晚,给下游模型的当前进度)

**已完成**:南向环境包全部配置并验证(见第 12 节);GitHub 仓库骨架已建好并推送([openharmony-env-monitor](https://github.com/NineSense9/openharmony-env-monitor),本地 `D:\实习\workspace`,含 app/device/docs 三目录、.gitignore、正式 README、本文档与日志周志模板)。

**本机配置**:Core Ultra 5 225H / 32GB 内存,并行跑虚拟机 + DevEco Studio 无压力。

**Hyper-V 结论(重要,勿动)**:本机 Hyper-V 处于开启状态,且 WSL2(Ubuntu-24.04)和 Docker Desktop 正在使用它,**不能关闭**(关了会废掉 WSL/Docker)。因此 VirtualBox 走 Hyper-V 兼容模式运行——已实测可正常启动,代价是 VM 内编译略慢,32G 内存的机器可接受。

**DevEco Studio(北向,01 号包缺失的补位)**:
- 官方当前稳定版 **5.0.5 Release**;下载页:https://developer.huawei.com/consumer/cn/deveco-studio/ (点"立即下载",需登录华为账号)
- 系统要求 Win10/11 64 位、16GB+ 内存(本机满足)
- 策略:8/31 若老师发了 01 号包,以老师的版本为准安装;否则自行下载 5.0.5。DevEco 首次启动需华为开发者账号登录激活。
- 北向工程以后直接建在仓库 `app/` 目录下。

**用户待办(周一开工前)**:
1. 下载安装 DevEco Studio(或等老师 01 号包)+ 确认华为开发者账号可登录
2. 手机安装**校友邦 APP** 并确认账号可登录(Day1 就要写日志)
3. GitHub 仓库 Settings → Collaborators 添加组员用户名(否则组员只能看不能推)
4. Day1 现场问清:VM 登录凭据、板卡串口驱动、烧录镜像与地址表、上下班考勤时间、分组定岗

## 14. 实际开发进度与独立实验记录规则(2026-08-31)

### 14.1 当前已完成

按照《授课文档.pdf》已完成南向基础链路：

- 3.4 准备源码：在 Ubuntu 中克隆 Lockzhiner RK2206 OpenHarmony 3.0 LTS 源码；
- 3.5 安装 `hb`：`ohos-build 0.4.3` 安装成功；
- 3.6 编译固件：`hb build -f` 成功生成 `Firmware.img`、`Firmware.md5` 和 `rk2206_db_loader.bin`；
- 3.8 烧录到真板：RKDevTool 下载和校验均达到 100%；
- 3.9 串口调试：UART 使用 115200，已观察到 OpenHarmony 内核启动日志和 `hiview init success`。

这份基础固件只作为编译、烧录和启动基线，不能等同于最终的环境监测业务。

### 14.2 后续实验保存方式

南向实验统一放在仓库 `device/labs/` 下，每个实验一个目录，之前的实验不覆盖：

```text
device/labs/00_bringup
device/labs/01_hello_world
device/labs/02_lab01_lcd
device/labs/03_lab02_key_lcd
device/labs/04_lab03_light_key_lcd
device/labs/05_lab04_mq2_key_lcd
device/labs/06_lab05_sht30_key_lcd
device/labs/07_lab06_multitask_lcd
device/labs/08_lab07_cabin_station
device/labs/09_lab08_wifi_ping
device/labs/10_cloud_telemetry
```

每个实验保存 `README.md`、独立源码、对基础源码的补丁或修改说明、编译/烧录/串口记录。完整 OpenHarmony 源码树、虚拟环境、数据库、密码和大体积构建产物不提交到仓库；README 中记录复现路径和镜像校验值。

每完成一个实验，必须同步更新实验目录、`device/labs/README.md`、本流程文档和进度 Markdown，然后使用清晰的 commit 提交并推送 GitHub，方便 Codex 与 Antigravity 协同。当前 `device/labs/01_hello_world` 已完成源码保存、编译、烧录和 UART 周期输出验收；本实验不包含 LCD 功能，板载显示屏无输出属于预期。根据授课文档第四章，当前进入 `02_lab01_lcd`，后续严格按
`lab01_lcd` → `lab02_key_lcd` → `lab03_light_key_lcd` → `lab04_mq2_key_lcd` →
`lab05_sht30_key_lcd` → `lab06_multitask_lcd` → `lab07_cabin_station` →
`lab08_wifi_ping` 顺序推进。

### 14.3 LCD 实验历史故障定位与降噪记录（2026-08-31）

`lab01_lcd` 诊断版 UART 已输出 `LCD_INIT_DONE` 和 `LCD_FILL_BEGIN`，
之后用户手动按下 `K1=RESET`。因此不能把该次串口断开记为设备自动复位；
当前确定的故障边界是全屏软件 SPI `lcd_fill()` 尚未完成。

为避免全屏刷屏长时间连续占用 CPU，历史分段刷屏版在 `lcd_fill()` 中每 8 行
输出一次进度并调用 `LOS_Msleep(1)`。该修改不改变授课文档 4.5 的完整调用
顺序，也不初始化或控制电机、蜂鸣器、RGB 和报警灯。

历史分段刷屏版：

- 目录：`D:\实习\tmp\rk2206_images\lab02_lab01_lcd_yield_20260831`
- `Firmware.img` MD5：`1060f2209ccb3706c8e2736ff9b67a9a`
- `rk2206_db_loader.bin` MD5：`5f2ea974b0e1df5564a8e1ee910627bb`
- 构建结果：`852/852`、`lockzhiner-rk2206 build success`
- 本地契约测试：`3 passed`
- 状态：已由下面的 SMART-R DC 引脚修正版替代，不再作为当前烧录版本

### 14.4 SMART-R LCD 引脚根因修复与新镜像（2026-08-31）

用户提供的 SMART-R 板背面照片显示 LCD 排针丝印为
`A4 / MISO / MOSI / CLK / CS / GND / 5V`。结合 RK2206 SDK 的 SPI0 M1
复用表，LCD 实际引脚为 `DC=GPIO0_PA4`、`RES=GPIO0_PC3`、
`MOSI=GPIO0_PC2`、`CLK=GPIO0_PC1`、`CS=GPIO0_PC0`。

此前沿用普通小凌派 `b4_lcd` 驱动，把 `DC` 错设为 `GPIO0_PC6`。SDK 明确
`GPIO0_PC6` 同时是 `PWM6`，授课文档 4.11 将 PWM6 分配给 SMART-R 电机，
所以 LCD 传输会让电机持续抖动；DC 接错也会使 LCD 一直空白。修复已将
`LCD_PIN_DC` 改为 `GPIO0_PA4`，并删除只能在刷屏结束后拉低 PC6 的临时接口。

本地契约测试为 `4 passed`。Ubuntu 独立工程增量构建返回
`BUILD_RC=0`、`lockzhiner-rk2206 build success`，完成 `20/20`；ELF 中
已无 `lcd_set_dc_idle` 符号。

新镜像单独保存，不覆盖历史版本：

- 目录：`D:\实习\tmp\rk2206_images\lab02_lab01_lcd_smart_r_a4_20260831`；
- `Firmware.img` MD5：`3fad85ef9a94dc22a831c5b7659149d6`；
- `rk2206_db_loader.bin` MD5：`5f2ea974b0e1df5564a8e1ee910627bb`；
- 详细记录：`device/labs/02_lab01_lcd/records/2026-08-31-build-smart-r-a4.md5`。

本次构建教训：全量 `hb build -f` 在 VirtualBox/Hyper-V 环境曾触发 Ubuntu
soft lockup，后续实验优先复用构建树做增量 `hb build`；同步源码必须分别指定
`lab01_lcd/`、`include/`、`src/` 目标路径，并在链接前检查旧符号，避免再次
出现 `undefined reference to lcd_set_dc_idle`。

当前状态：修正版已构建并校验，等待用户烧录后确认 LCD 三行英文显示和电机停止
抖动；在用户反馈前不标记为物理验收通过。

### 14.5 SMART-R LCD 方向与速度修复（2026-08-31）

用户已确认上一版 `SMART-R A4 DC` 镜像的电机不再抖动，LCD 已显示正常三行文字；
但实物画面方向反了，且全屏显示速度较慢。经核对授课文档第 4.4.2、4.5 节和
当前驱动：

- 旧版 `USE_HORIZONTAL=2` 使用横屏 `MADCTL=0x70`，与 SMART-R LCD 的实际安装
  方向不一致；已切换为 `USE_HORIZONTAL=0` 竖屏，保留 PDF 规定的左上角坐标语义；
- 旧版 `LCD_ENABLE_SPI=0`，全屏 `lcd_fill()` 通过 GPIO 逐位模拟 SPI，导致
  刷屏慢；已启用 SDK SPI0 M1 硬件 SPI；
- 硬件 SPI 时序使用 `SPI_MODE_0`，因为旧模拟实现是在时钟低电平准备数据、
  上升沿完成采样；速度配置沿用驱动中的 50 MHz；
- 全屏填充改为每行批量发送 RGB565 缓冲区，同时局部矩形填充按实际宽度发送，
  兼容后续实验擦除局部文字；
- `DC=GPIO0_PA4` 和 `CS/CLK/MOSI/RES=GPIO0_PC0/PC1/PC2/PC3` 保持不变，
  不再触碰 `GPIO0_PC6/PWM6`，因此不会恢复此前的电机抖动。

本地契约测试结果：`5 passed`。Ubuntu 独立工程增量构建成功，输出
`lockzhiner-rk2206 build success`，`liblab01_lcd.a` 参与链接。

新版本独立保存：

- 镜像目录：`D:\实习\tmp\rk2206_images\lab02_lab01_lcd_portrait_hwspi_20260831`；
- `Firmware.img` MD5：`7dcff693bfe9fdfe61ba579a2377142d`；
- `rk2206_db_loader.bin` MD5：`5f2ea974b0e1df5564a8e1ee910627bb`；
- 详细记录：`device/labs/02_lab01_lcd/records/2026-08-31-build-portrait-hwspi.md5`。

该版本等待用户烧录确认方向和刷新速度。烧录仍严格按授课文档使用
`K2=MASKROM`，完成后退出下载模式并使用 `K1=RESET` 重启；验收同时记录 UART
的 `lab01_lcd: LCD OK` 和电机状态。

### 14.6 SMART-R LCD 竖屏 180° 方向修复（2026-08-31）

用户反馈上一版 `USE_HORIZONTAL=0` 已实际烧录，LCD 能正常显示且电机没有抖动，
但文字起始位置仍在屏幕右下角。该结果说明硬件 SPI 和 DC 引脚修复已生效，
剩余问题是 LCD 控制器物理原点与普通竖屏方向相反。

本次将 `USE_HORIZONTAL` 改为 `1`。根据现有驱动方向分支，这会发送
`MADCTL=0xC0`，即对竖屏显示旋转 180°，使逻辑 `(0,0)` 映射到物理左上角。
硬件 SPI、`SPI_MODE_0`、`DC=GPIO0_PA4`、SPI0 M1 和逐行批量刷屏保持不变。

本地契约测试结果：`5 passed`；方向测试已记录旧值 `0` 失败、改为 `1` 后通过。
Ubuntu 独立工程预检确认 `USE_HORIZONTAL=1` 后构建成功，输出
`lockzhiner-rk2206 build success`。

新版本单独保存：

- 镜像目录：`D:\实习\tmp\rk2206_images\lab02_lab01_lcd_portrait_reverse_hwspi_20260831`；
- `Firmware.img` MD5：`a0035dd02c5af006199a483883e7c5be`；
- `rk2206_db_loader.bin` MD5：`5f2ea974b0e1df5564a8e1ee910627bb`；
- 详细记录：`device/labs/02_lab01_lcd/records/2026-08-31-build-portrait-reverse.md5`。

该版本等待用户烧录确认。仍严格按授课文档使用 `K2=MASKROM`，完成后退出下载
模式并使用 `K1=RESET` 重启，同时记录 UART 的 `lab01_lcd: LCD OK` 和电机状态。

### 14.7 SMART-R LCD 横屏方向 3 修正（2026-08-31）

用户实际烧录 `USE_HORIZONTAL=1` 的竖屏 180°镜像后反馈：显示仍从 LCD 右下角开始
且方向反了，并提供板上原厂程序截图作为正确显示位置参照。重新对照授课文档
第 4.4.2、4.5 节确认，前面把问题判断为竖屏是错误的：PDF 公共 LCD 坐标明确为
横屏 `LCD_W=320`、`LCD_H=240`，`lcd_show_string(10,40,...)` 应从字左上角开始。

本次将 `include/lcd.h` 恢复到横屏尺寸并设为 `USE_HORIZONTAL=3`。现有方向分支
会发送 `MADCTL=0xA0`，用于修正原 `USE_HORIZONTAL=2` 的 `MADCTL=0x70` 方向；
DC=`GPIO0_PA4`、SPI0 M1、`SPI_MODE_0`、逐行批量刷屏和不占用电机 `PWM6` 的修复
保持不变。此前 `USE_HORIZONTAL=0/1` 的竖屏尝试保留为失败记录，后续不得沿用。

本地契约测试：`5 passed`。Ubuntu 独立工程增量构建成功，输出
`lockzhiner-rk2206 build success`。

新镜像独立保存：

- 目录：`D:\实习\tmp\rk2206_images\lab02_lab01_lcd_landscape_orientation3_hwspi_20260831`；
- `Firmware.img` MD5：`e658972f997d43bb16d056fc6eb329c1`；
- `rk2206_db_loader.bin` MD5：`5f2ea974b0e1df5564a8e1ee910627bb`；
- 详细记录：`device/labs/02_lab01_lcd/records/2026-08-31-build-landscape-orientation3.md5`。

该版本等待用户烧录确认。烧录仍按 PDF 使用 `K2=MASKROM`，完成后退出下载模式
并使用 `K1=RESET` 重启；确认板子正向摆放后，验收文字从 LCD 左上角开始、方向正常、
刷屏速度较快且电机保持静止。

### 14.8 SMART-R LCD 横屏方向 3 硬件 SPI 模式 3 修正（2026-08-31）

用户烧录上一版横屏方向 3 镜像后反馈 LCD 仍从右下角开始。此前连续调整
`USE_HORIZONTAL` 和 `MADCTL` 不能解决问题，说明根因不在方向宏本身。对比 SDK
原始 `b4_lcd` 以及 RK2206 SPI 适配文档后发现：原始 LCD 硬件 SPI 参考实现使用
`SPI_MODE_3`，而上一版项目代码根据 GPIO 模拟 SPI 的边沿直觉使用了
`SPI_MODE_0`。RK2206 硬件抽象层中软件模拟边沿和硬件模式不能直接等同，模式 0
可能导致 LCD 显示部分内容，但不能可靠接收 `MADCTL` 等初始化命令。

本次只将 `src/lcd.c` 的硬件 SPI 模式恢复为 SDK 参考的 `SPI_MODE_3`，以下配置
保持不变：横屏 `USE_HORIZONTAL=3`、`LCD_W=320`、`LCD_H=240`、DC=`GPIO0_PA4`、
SPI0 M1、50 MHz、批量逐行刷屏以及唯一 `APP_FEATURE_INIT` 启动入口。TDD 契约在
旧模式上先得到 `1 failed, 4 passed`，恢复实现后得到 `5 passed`；Ubuntu 独立工程
输出 `lockzhiner-rk2206 build success`。

新镜像单独保存：

- 镜像目录：`D:\实习\tmp\rk2206_images\lab02_lab01_lcd_landscape_orientation3_hwspi_mode3_20260831`；
- `Firmware.img` MD5：`6d3cf42e42146864862b0e8fdb8d2f38`；
- `rk2206_db_loader.bin` MD5：`5f2ea974b0e1df5564a8e1ee910627bb`；
- 详细记录：`device/labs/02_lab01_lcd/records/2026-08-31-build-landscape-orientation3-mode3.md5`。

该版本尚未完成物理验收。烧录按授课 PDF 使用 `K2=MASKROM`，完成后退出下载模式
并使用 `K1=RESET` 重启；记录 UART 的 `lab01_lcd: LCD OK`、LCD 是否从左上角开始、
方向是否正常以及电机是否保持静止。

### 14.9 SMART-R LCD 正常横屏 `MADCTL=0x60` 修正（2026-08-31）

用户烧录 `SPI_MODE_3` + `MADCTL=0xA0` 版本后确认 LCD 仍从右下角、从下往上加载。
这与 `0xA0` 的反向横屏扫描一致。本次将默认方向改为 `USE_HORIZONTAL=2`，由方向
分支发送正常横屏 `MADCTL=0x60`（`MX|MV`），保持 `SPI_MODE_3`、PA4 DC、SPI0 M1、
50 MHz、批量刷屏和唯一启动入口不变。

本地契约测试为 `5 passed`，Ubuntu 独立工程构建成功。新镜像尚未完成物理验收，
烧录后仍需确认 LCD 从左上角显示、方向正常、电机静止。

### 14.10 SMART-R LCD 最终链接产物修复（2026-08-31）

用户烧录 `0x60` 版本后反馈 LCD 位置仍未变化。复核构建树后确认，源码和
`liblab01_lcd.a` 已更新，但最终 `liteos.elf` 仍是较早时间生成的旧文件，
之前的镜像因此没有实际包含最新方向配置。删除过期 ELF 和镜像后重新执行
`hb build`，并检查反汇编：`lcd_init` 在发送命令 `0x36` 后加载的立即数为
`96`，即 `MADCTL=0x60`。重新链接后的 ELF 和镜像时间分别更新到 19:25。

重新链接版单独保存：

- 目录：`D:\实习\tmp\rk2206_images\lab02_lab01_lcd_landscape_normal_hwspi_mode3_relinked_20260831`；
- `Firmware.img` MD5：`89fb0ba476d3cecb516dd7371c5e0bec`；
- `rk2206_db_loader.bin` MD5：`5f2ea974b0e1df5564a8e1ee910627bb`；
- 记录：`device/labs/02_lab01_lcd/records/2026-08-31-build-landscape-normal-relinked.md5`。

后续验收按本 PDF 使用 `K2=MASKROM` 烧录，完成后退出下载模式并按 `K1=RESET`
重启。当前只认重新链接版目录；旧的 `lab02_lab01_lcd_landscape_normal_hwspi_mode3_20260831`
目录中的镜像不再作为验收版本。

### 14.11 SMART-R LCD 方向问题阶段性收口（2026-08-31）

用户实际烧录重新链接版镜像后确认：LCD 仍从右下角开始，文字方向倒置，和此前
版本相比没有实物变化。当前版本的 LCD 初始化、刷屏和实验文字显示均可运行；按
协作决定，暂不继续消耗时间排查 LCD 物理方向，先接受该现象，不阻塞后续实验。
后续实验从 `lab02_lab01_lcd_landscape_normal_hwspi_mode3_relinked_20260831`
对应的代码基线复制到各自独立目录，并在新目录中继续开发。

### 14.12 LCD 历史镜像归档（2026-08-31）

为避免后续烧录选错版本，`D:\实习\tmp\rk2206_images` 主目录只保留
`lab02_lab01_lcd_landscape_normal_hwspi_mode3_relinked_20260831`。之前生成的
11 个 `lab02_lab01_lcd*` 镜像目录已完整移动到
`D:\实习\tmp\rk2206_images\archive\2026-08-31-lcd-trials`，没有删除历史文件。
归档明细见 `device/labs/02_lab01_lcd/records/2026-08-31-image-archive.md`。

### 14.13 lab02_key_lcd 源码与构建完成（2026-08-31）

按照授课文档第 4.6 节进入按键 + LCD 实验。新实验目录为
`device/labs/03_lab02_key_lcd`，只读取 K3=`GPIO0_PC7`，按下为低电平；K1 是
RESET，K2 是 MASKROM，不作为普通按键。程序每 30 ms 轮询 K3，只有状态变化时
更新 LCD 和 UART，且不访问电机 PWM6=`GPIO0_PC6`。

4.5 的 LCD 方向倒置问题作为已知基线继承，4.6 不再修改 LCD 方向寄存器。当前
已完成源码、契约测试和 Ubuntu 独立构建；本地测试为 `4 passed`，构建完成
`853/853`，输出 `lockzhiner-rk2206 build success`。固件和 Loader 已生成到
`D:\实习\tmp\rk2206_images\lab03_lab02_key_lcd_20260831`，Firmware MD5 为
`5ed8e34a6069dee30ebd8bd83bf90919`，Loader MD5 为
`5f2ea974b0e1df5564a8e1ee910627bb`。

本次构建遇到并记录了两项可复用问题：复制基线会带入旧 `out/` 导致 `hb env`
指向 4.5，以及多源 `scp` 会将 `include/`、`src/` 文件扁平化。已删除新工程
`out/`、重新执行 `hb set -root .`/`hb set -p`，并恢复远端目录结构后构建成功。
当前只完成源码、测试和构建，尚未进行 K3 实物烧录验收；实际 UART、LCD 状态和
电机状态待用户烧录后补录。详细说明见 `device/labs/03_lab02_key_lcd/README.md`。

### 4.9 lab02_key_lcd K3 读取失败修复与重试构建（2026-08-31）

用户烧录初始 4.6 包后反馈 LCD 仍为 4.5 的已知倒置画面，且 K3 没有反应。复核
确认用户消息中的路径 `D:\实习\tmp\rk2206\_images\lab03\_lab02\_key\_lcd\_20260831`
在 Windows 上不存在；实际初始包目录为
`D:\实习\tmp\rk2206_images\lab03_lab02_key_lcd_20260831`，其
`Firmware.img` MD5 为 `5ed8e34a6069dee30ebd8bd83bf90919`。这解释了为何反复
烧录时看到之前的 LCD 画面：烧录目录仍是初始包，且 LCD 方向本来就继承了 4.5
的阶段性基线。

进一步检查 4.6 代码发现，`tx_key_is_pressed()` 首次 GPIO 读取失败后，业务任务
直接返回，导致欢迎文字能显示但 K3 状态行和后续轮询都不会出现。修复内容为：

- K3 pinctrl 按 SDK 示例改用 `PULL_KEEP`；
- 读取失败时显示 `K3: READ ERR` 并保留任务；
- 读取失败期间每 100 ms 重试，恢复后继续 30 ms 状态轮询；
- 增加契约测试，确保失败可见且可重试。

修复后 Ubuntu 独立工程使用显式 PATH 执行 `hb build -f`，完成 `853/853`，输出
`lockzhiner-rk2206 build success`，新镜像 MD5 为
`df7f5e3fbb3926bcbc08e3d657453a59`。当时唯一待烧录目录为
`D:\实习\tmp\rk2206_images\lab03_lab02_key_lcd_retry_20260831`，初始包目录
保留为历史记录，不再用于本次验收；该目录现已由下方 `PULL_UP` 修正版替代。
待用户烧录后补录 K3、LCD、UART 和电机状态。

### 4.10 K3 pinctrl 配置修正（2026-08-31）

用户提供的 UART 截图实际显示：

```text
lab02_key_lcd: K3 pinctrl failed ret=1
lab02_key_lcd: tx_key_init failed(1)
```

根因是上一节构建使用的 `PULL_KEEP` 在 `GPIO0_PC7` 的 `PinctrlSet` 阶段返回
失败，任务尚未进入 K3 读取循环。该重试包 `Firmware.img` MD5 为
`df7f5e3fbb3926bcbc08e3d657453a59`，现标记为作废；这也解释了用户截图中
“K3 没反应”的现象。

当时已将源码和契约测试改为 `PULL_UP`，并在 Ubuntu 独立工程重新执行
`hb build -f`，完成 `853/853`，输出 `lockzhiner-rk2206 build success`，
`BUILD_RC=0`。该版本后来经实物验证仍失败，当前唯一可烧录目录见下方
4.11 节；本节目录仅保留作历史记录。
原 PULL_UP 包目录为
`D:\实习\tmp\rk2206_images\lab03_lab02_key_lcd_pullup_20260831`，其
`Firmware.img` MD5 为 `5d5360c5bcea67ec33f0c30924e27f4a`，Loader MD5 为
`5f2ea974b0e1df5564a8e1ee910627bb`。烧录后按 PDF 使用 K2=MASKROM，退出下载
模式后用 K1=RESET，再观察 UART 是否出现 `K3=RELEASED`、`K3=PRESSED`。

本次修正已复制独立产物、构建日志和 MD5 记录；在用户完成上板验证前，不标记 K3
已修复。

### 4.11 K3 无额外 pinctrl 构建（2026-08-31）

用户随后烧录 `PULL_UP` 包仍得到同一组 UART：

```text
lab02_key_lcd: K3 pinctrl failed ret=1
lab02_key_lcd: tx_key_init failed(1)
```

因此 `PULL_KEEP` 和 `PULL_UP` 都标记为作废。结合授课文档 4.6 只要求将 K3
`GPIO0_PC7` 设为输入，以及截图中 `LzGpioInit` 已成功，失败边界确定为额外的
`PinctrlSet` 调用，而不是烧录文件或 K3 轮询逻辑。

修复前先新增契约测试，要求 `src/tx_key.c` 不调用 `PinctrlSet`，确认旧源码得到
`1 failed, 4 passed`；随后删除该调用及其错误分支，只保留 `LzGpioInit`、
`LzGpioSetDir(..., LZGPIO_DIR_IN)` 和 `LzGpioGetVal`。本地测试结果为 `5 passed`。

在 Ubuntu 独立工程执行 `hb build -f`，完成 `853/853`，`BUILD_RC=0`，并复制到
新目录：

```text
D:\实习\tmp\rk2206_images\lab03_lab02_key_lcd_no_pinctrl_20260831
```

`Firmware.img` MD5 为 `4bcd54b6926794a78620fd7c30f64abf`，Loader MD5 为
`5f2ea974b0e1df5564a8e1ee910627bb`。新 ELF 和固件中均未发现
`K3 pinctrl failed` 字符串。该 `no_pinctrl` 包作为无额外 pinctrl 的历史基线保留；
当前改用下方原始电平诊断包，详细记录见 `device/labs/03_lab02_key_lcd/README.md`。

### 4.12 K3 原始电平诊断构建（2026-08-31）

用户上板反馈当前无额外 pinctrl 版本已经能显示 `LCD OK` 和 `K3: PRESSED`，但按下
K3 没有状态变化。此现象已证明 LCD 初始化、K3 初始化和至少一次 PC7 读取已经执行，
但仅看状态文字无法区分启动时按键处于按下、PC7 始终为低、PC7 浮空或实体按键未接入
当前输入等情况。

按照系统调试流程，先不修改 PDF 规定的 `GPIO0_PC7`，不改为 ADC，也不重新加入已经
实测失败的额外 `PinctrlSet`。在 `tx_key` 驱动中增加原始电平读取接口，业务仍以 30 ms
周期识别低电平按下，同时每 500 ms 输出：

```text
lab02_key_lcd: K3 raw=%u pressed=%u
```

诊断固件已独立保存到：

```text
D:\实习\tmp\rk2206_images\lab03_lab02_key_lcd_diagnostic_20260831
```

`Firmware.img` MD5 为 `95f25a65162f806cdf1e66ada6436799`，Loader MD5 为
`5f2ea974b0e1df5564a8e1ee910627bb`。Ubuntu 全量构建为 `853/853`，输出
`lockzhiner-rk2206 build success`。待用户先松开 K3，再按住和松开 K3 并提供完整
UART，根据 raw 值决定后续是修正电气配置、核对按键映射还是修正业务判断；在此之前
不将 K3 标记为最终验收完成。详细记录见 `device/labs/03_lab02_key_lcd/README.md`。

### 4.13 lab02_key_lcd K3-K6 复测边界（2026-09-01）

用户在当前 K3 原始电平诊断包上依次按下 K3、K4、K5、K6，未观察到 LCD 或串口状态变化。
现场 UART 持续输出 `lab02_key_lcd: K3 raw=0 pressed=1`。根据授课文档 4.3 和 4.6，
本课程例程只使用 K3=`GPIO0_PC7`；K4-K6 在本实验没有读取逻辑，所以没有响应属于预期，
不能据此修改程序。

K3 持续为 `raw=0` 只能说明当前读取到低电平，尚不能区分按键未松开、PC7 被硬件持续拉低、
按键连线与 PDF 映射不一致或板级复用异常。后续验收必须先完全松开所有按键并复位，确认
`raw` 是否变为 `1`，再单独按住、松开 K3；在此之前不修改 `GPIO0_PC7` 映射，也不把 K3
标记为已修复。详细现场记录见 `device/labs/03_lab02_key_lcd/records/2026-08-31-uart.txt`。

### 4.14 ADC5 诊断包同步问题修正（2026-09-01）

用户反馈烧录 ADC5 诊断包后“没有变化”。复核发现 Ubuntu 端已在 2026-09-01
01:28 生成新 `Firmware.img`，但 Windows 待烧录目录仍是 2026-08-31 23:44 的旧文件，
所以实物反馈对应旧包，不能据此否定 ADC5 代码。现已将服务器最新产物同步覆盖到：

```text
D:\实习\tmp\rk2206_images\lab03_lab02_key_lcd_diagnostic_20260831
```

并核对 MD5：`Firmware.img` 为
`35a412a1ef7e220efbf14bbc843994af`，`rk2206_db_loader.bin` 为
`5f2ea974b0e1df5564a8e1ee910627bb`。后续每次烧录必须同时核对目录和 MD5，不能只
依据同名文件判断版本。最新构建完成 `852/852`，最终链接库为
`liblab02_key_lcd_adc_diagnostic.a`。具体构建、同步和复测步骤见
`device/labs/03_lab02_key_lcd/records/2026-09-01-build.md` 与
`device/labs/03_lab02_key_lcd/records/2026-09-01-retest.md`。

### 4.15 ADC5+PC7 输入矩阵诊断包重新构建（2026-09-01）

继续复核后确认，4.14 中提到的 `lab03_lab02_key_lcd_diagnostic_20260831`
目录仍是旧诊断包：Windows 侧 `Firmware.img` 可检索到 `key=KEY_UNKNOWN`，但没有
`K3_GPIO=GPIO0_PC7`、`ADC5 raw=` 和 `PC7 raw=`。因此该目录保留为错误同步对照，
不要继续烧录。

进入 Ubuntu VM 时曾出现 `.bashrc`、`df`、`scp`、`mount` 的 I/O error 或段错误，已通过
VirtualBox 关机并重新启动恢复。恢复后重新同步本地诊断源码，执行 `hb build -f`，构建
完成 `852/852`，输出 `lockzhiner-rk2206 build success`。构建日志中可见
`liblab02_key_lcd_adc_diagnostic.adc5_key_lcd.o`、`liblab02_key_lcd_adc_diagnostic.lcd.o`
和 `liblab02_key_lcd_adc_diagnostic.a`，最终链接库未包含旧 `lab02_key_lcd` 或 `lab01_lcd`。

新烧录目录为：

```text
D:\实习\tmp\rk2206_images\lab03_lab02_key_lcd_input_matrix_20260901
```

该目录中的 `Firmware.img` MD5 为 `ab8ee95e8d116c0705a46409baba77b0`，
`rk2206_db_loader.bin` MD5 为 `5f2ea974b0e1df5564a8e1ee910627bb`。Windows 端已确认
`Firmware.img` 包含 `K3_GPIO=GPIO0_PC7`、`ADC5 raw=`、`PC7 raw=`，且不包含
`key=KEY_UNKNOWN`。下一次上板只烧录此新目录，烧录后保持 K3-K6 全部松开并复位，
再分别按住、松开 K3、K4、K5、K6，每个动作保持约 2 秒并保存完整 UART。

### 4.16 Pin Sniffer 定位 K3 与 4.6 布局修正（2026-09-01）

用户烧录 UART-only Pin Sniffer 后，未按键时 `ADC3` 在约 `410-458` raw 之间持续抖动，
判定为浮动/噪声，不作为按键映射依据。K3 按下和抬起时，UART 明确捕捉到
`GPIO0_PC7` 的 `1->0` 与 `0->1`：

```text
pin_sniffer: GPIO_CHANGE name=GPIO0_PC7 id=23 1->0 tick=5770
pin_sniffer: GPIO_CHANGE name=GPIO0_PC7 id=23 0->1 tick=5776
```

结论：当前实物 K3 与授课文档 4.6 一致，使用 `GPIO0_PC7`，低电平表示按下。正式
`tx_key.c` 保留 K3 调用方式：先 `LzGpioInit(TX_KEY_K3)`，再尝试
`PinctrlSet(TX_KEY_K3, MUX_FUNC0, PULL_KEEP, DRIVE_LEVEL0)`，该返回值只作为 warning
记录，不作为失败退出条件，随后继续 `LzGpioSetDir(..., LZGPIO_DIR_IN)` 和
`LzGpioGetVal`。该调用方式后续实验继续复用。

Pin Sniffer 诊断包保留在：

```text
D:\实习\tmp\rk2206_images\lab03_lab02_key_lcd_pin_sniffer_20260901
```

`Firmware.img` MD5 为 `4824acbf9b81ae8b37cf6fcceb1e499a`，Loader MD5 为
`5f2ea974b0e1df5564a8e1ee910627bb`。

随后用户烧录 PC7 正式版，LCD 已能在 `K3: RELEASED` 与 `K3: PRESSED` 间切换。照片
显示 `K3` 状态刷新区域会影响上方 `LCD OK`，根因是旧状态行 `y=96` 和擦除区
`y=90-120` 与 `LCD OK` 行重叠。已将状态行下移到 `y=160`，擦除区改为 `y=152-184`。

当前 4.6 推荐烧录目录为：

```text
D:\实习\tmp\rk2206_images\lab03_lab02_key_lcd_pc7_layout_20260901
```

`Firmware.img` MD5 为 `483bf670889e886a2ce373ccf0b3aa53`，Loader MD5 为
`5f2ea974b0e1df5564a8e1ee910627bb`。Ubuntu 构建为 `853/853`，输出
`lockzhiner-rk2206 build success`。当前遗留问题：LCD 物理方向仍继承 4.5 的倒置基线；
### 4.17 实验3：灯 + 按键 + LCD（lab03_light_key_lcd）完成构建与实物验收（2026-09-01）

按照《授课文档》第 4.7 节要求完成 `04_lab03_light_key_lcd` 独立实验：
- 启用 `-llab03_light_key_lcd`，实现 `tx_light.c`（控制 PA5 告警灯输出）与 `tx_key_click`（K3 PC7 下降沿单次短按翻转触发）；
- LCD 屏幕实时更新 `Light: ON`（红字）与 `Light: OFF`（黑字）；
- 串口打印 `lab03_light_key_lcd: K3 clicked -> Light: ON/OFF`；
- 在独立编译树 `lab04-lab03-light-key-lcd-20260901` 中完成全量编译 `854/854`，生成无旧代码残留的干净固件。

烧录目录为：
```text
D:\实习\tmp\rk2206_images\lab04_lab03_light_key_lcd_20260901
```
- `Firmware.img` MD5: `bf891d8f00f72c5cd82af3b0abc91845`
- `rk2206_db_loader.bin` MD5: `5f2ea974b0e1df5564a8e1ee910627bb`
- **实物验收结果**: 【验收通过】用户实测短按 K3 成功触发告警灯亮灭切换，LCD 状态与物理 PA5 告警灯完全同步，串口日志输出正常。

### 4.18 实验4：MQ2 气体 + 按键 + LCD（lab04_mq2_key_lcd）完成构建与实物验收（2026-09-01）

按照《授课文档》第 4.8 节要求完成 `05_lab04_mq2_key_lcd` 独立实验：
- 启用 `-llab04_mq2_key_lcd`，实现 `mq2.c`（基于 SARADC 通道 4，引脚 `GPIO0_PC4`，配合 `DevIoInit` 与 `LzSaradcReadValue` 进行模拟电压采集与 PPM 浓度公式换算）；
- 实现 `mq2_ppm_calibration()` 动态校准当前空气基准值 `r0`；
- LCD 屏幕实时动态显示 `Gas: xx.x ppm` 与校准状态 `Cal: READY / DONE`；
- K3 按键（`GPIO0_PC7`）短按触发基准值重新校准；
- 在独立编译树 `lab05-lab04-mq2-key-lcd-20260901` 中完成全量编译 `854/854`，生成干净固件。

烧录目录为：
```text
D:\实习\tmp\rk2206_images\lab05_lab04_mq2_key_lcd_20260901
```
- `Firmware.img` MD5: `159b47e6c181039d698f799dcd8624bb`
- `rk2206_db_loader.bin` MD5: `5f2ea974b0e1df5564a8e1ee910627bb`
- **实物验收结果**: 【验收通过】用户实测 Gas PPM 实时刷新正常，吹气读数变化灵敏，K3 短按成功触发重新校准与状态提示。

### 4.19 实验5：SHT30 + 按键 + LCD（lab05_sht30_key_lcd）完成构建与实物验收（2026-09-01）

按照《授课文档》第 4.9 节要求完成 `06_lab05_sht30_key_lcd` 独立实验：
- 启用 `-llab05_sht30_key_lcd`，实现 `sht30.c`（基于 I2C0 通道，引脚 SCL=`GPIO0_PA1`、SDA=`GPIO0_PA0`，地址 `0x44`，发送周期测量指令与回读 6 字节温湿度，并经过 CRC8 校验换算真实温湿度）；
- 实现 LCD 屏幕实时更新 `Temp: xx.x C` 与 `Humi: xx.x %`；
- K3 按键（`GPIO0_PC7`）短按触发 `freeze` 状态翻转（LCD 对应显示 `Freeze: OFF` / `Freeze: ON`，冻结时保留当前读数停止刷新）；
- 在独立编译树 `lab06-lab05-sht30-key-lcd-20260901` 中完成全量编译 `854/854`，生成干净固件。

烧录目录为：
```text
D:\实习\tmp\rk2206_images\lab06_lab05_sht30_key_lcd_20260901
```
- `Firmware.img` MD5: `3f18a1654466fcc7b3ade57780c06273`
- `rk2206_db_loader.bin` MD5: `5f2ea974b0e1df5564a8e1ee910627bb`
- **实物验收结果**: 【验收通过】用户实测温湿度动态采样真实准确，捂传感器温湿度快速响应上升，K3 短按成功冻结/解冻屏幕刷新。

### 4.20 实验6：多任务 + LCD（lab06_multitask_lcd）完成构建（2026-09-01）

按照《授课文档》第 4.10 节要求完成 `07_lab06_multitask_lcd` 独立实验：
- 启用 `-llab06_multitask_lcd`，使用 LiteOS-M 创建双任务并行调度系统：
  - `lab06_sample_task`（周期 3000ms）：负责慢速 I2C 读取 SHT30 温湿度，更新共享 `g_temp`/`g_humi`；
  - `lab06_ui_task`（周期 200ms）：负责快速自增 `g_tick`、实时刷新 LCD（包括 `Tick: %u`、温湿度与 `Freeze` 状态）、监听 K3 边沿翻转 `g_freeze`；
- 当处于冻结状态时，采样任务暂停更新温湿度数据，但 UI 任务的 `Tick` 计数依然以 200ms 高频递增，直观证明任务调度互不阻塞；
- 在独立编译树 `lab07-lab06-multitask-lcd-20260901` 中完成全量编译 `854/854`，生成干净固件。

烧录目录为：
```text
D:\实习\tmp\rk2206_images\lab07_lab06_multitask_lcd_20260901
```
- `Firmware.img` MD5: `6bab15719f7e847f1d1951a83e308f12`
- `rk2206_db_loader.bin` MD5: `5f2ea974b0e1df5564a8e1ee910627bb`

## 附录 A:实习方案 PDF 原文全文(供下游模型直接引用)

**P1** 太空空间站内部环境监测系统全链路开发实训方案

**P2 一、实训概述**:本实训以 OpenHarmony 南北向全栈开发为核心,基于鸿蒙南向开发板与空间站内部环境监测传感器模块(气压、温度、湿度、风速),让学生掌握鸿蒙 APP 界面开发、数据可视化、远程控制、云端对接等北向技能,同时掌握南向驱动开发、传感器采集、自动/手动控制、Wi-Fi 数据上传等硬件开发能力。实训以端云一体太空空间站内部环境监测系统为统一项目成果,实现舱内环境数据采集、本地显示、自动控制、远程监控、阈值告警全流程闭环。通过分组协作、模块化开发、全链路联调与答辩验收,全面提升学生鸿蒙应用开发、嵌入式编程、物联网通信与工程实践能力。实训全程对标工程教育专业认证九大教学目标,以航天空间站监测真实复杂工程问题为载体,构建分层递进式工程实践教学体系,覆盖数理工程知识应用、复杂问题分析、系统方案设计、工程实验研究、现代开发工具使用、工程社会责任、可持续设计、职业伦理规范、团队协作与终身学习全部能力维度。实训产出全套标准化工程文档、完整软硬件项目源码、过程性评价记录。

**P2 二、实训对象**:1.专业方向:人工智能、计算机类、软件技术、物联网等相关专业。2.基础要求:至少掌握 1 门编程语言(Python/Java/C/HTML 等);了解面向对象编程思想;具备基本逻辑思维与问题分析能力。3.素质要求:态度端正、遵守纪律、具备团队协作意识,愿意从事 IT/移动开发方向职业发展。

**P2–3 三、教学目标**:九条(与本文第 7 节一致,略)。

**P3–4 四、实训模式与特色**:(一)实训模式:1.真实项目驱动式学习:以太空空间站舱内环境监测端云一体化系统作为唯一贯穿项目,学生分阶段完成需求、设计、编码、测试、部署全流程,在真实业务场景下落地复杂工程解决方案。2.三段分层递进推进:第一阶段鸿蒙北向 APP 开发;第二阶段鸿蒙南向嵌入式开发;第三阶段全链路联调、文档撰写、答辩,由软件到硬件、分模块到整体集成。3.小组定岗分工协作:4-6 人一组,设置项目经理、嵌入式工程师、鸿蒙应用工程师、测试工程师、文档工程师,模拟企业真实岗位分工。4.理论实操融合教学:每日理论知识点讲解 + 上机实操两大环节。(二)实训特色:1.产业双师产教融合体系:依托软通动力鸿蒙、物联网一线工程师组建教学团队,实训流程、技术标准完全对标企业真实项目。2.国产化航天特色实训项目:基于 OpenHarmony 国产系统、空间站航天场景,成果可直接作为工程教育认证特色实践支撑材料。3.软硬全栈复合技术覆盖:覆盖嵌入式底层驱动、鸿蒙 ArkTS 应用、MQTT 云端通信、数据可视化、自动控制多技术领域。4.技术+职业素养一体化培养:配套工程伦理、项目管理、答辩。

**P4–5 五、企业岗位职责及职业能力**(与本文第 5 节表格一致,略)

**P5–7 六、实训内容与进度**(十天课程表,与本文第 4 节逐日内容一致,原文细节为:Day1 DevEcoStudio、SDK、模拟器部署/ArkTS 基础与 APP 页面框架搭建/拆解空间站系统需求,输出需求文档;Day2 实时数据卡片、异常告警 UI 开发/ECharts 环境曲线图表集成/模拟数据动态刷新调试;Day3-4 MQTT 协议、接口封装、权限配置/云端环境数据拉取、断网容错逻辑/历史数据可视化模块开发;Day5 远程指令下发、操作日志存储/自定义温湿度、气压、风速告警阈值/异常弹窗提醒功能实现;Day6 代码重构、性能优化、单元测试/Bug 闭环、APP 打包交付/北向阶段成果自查与提交;Day7 Docker 鸿蒙编译环境搭建/硬件原理图研读、IO 接口适配/底层工程编译、烧录验证;Day8 温湿度、气压、风速底层驱动/多组传感器精度对照实验/原始数据采集、误差分析校准;Day9 SPI 本地屏幕、阈值自动调控、声光告警/WiFi 数据上传云端/终端功耗测试、休眠节能方案落地;Day10 端云全流程闭环综合测试/全套工程、实验、安全、可持续文档撰写/小组演示、答辩评审、求职辅导)

**P7–8 (二)项目介绍**:太空空间站内部环境监测端云一体化系统基于 OpenHarmony 南北向全栈架构搭建,覆盖感知层、鸿蒙终端层、网络层、云端服务层、鸿蒙 APP 应用层五大链路,实现舱内温湿度、气压、风速实时监测、本地自动调控、远程可视化管控、多级故障告警全闭环。核心功能模块:1.多传感器数据采集模块:实时采集环境数据,完成滤波、误差校准;2.本地终端控制模块:自动阈值调控、声光告警、SPI 本地屏、低功耗休眠;3.WiFi-MQTT 双向通信模块:数据上传、远程指令下发、断网缓存恢复;4.鸿蒙 APP 可视化模块:实时数据卡片、历史曲线、异常标红、告警弹窗;5.远程管控模块:自定义告警阈值、远程启停执行设备、操作日志记录;6.工程拓展模块:设备功耗统计、实验数据导出、系统轻量化优化。核心技术指标:传感器采集误差<3%,远程指令响应<500ms,断网缓存支持 24 小时历史数据存储。

## 附录 B:老师通知原文

> @全体成员 各位同学:生产实习是专业人才培养的一个重要的环节,是我们同学了解社会、接触生产实际,获取、掌握生产现场相关知识的重要途径。
> 为了保障实习教学的质量和效果,学院邀请了华清远见和软通动力两家企业的技术老师来校为我们同学开展为期 2 周的集中实习教学。实习从下周一(8 月 31 日)正式开始,有以下几点要求应认真完成:
> (1) 按照实习单位规定的工作要求准时出勤。
> (2) 积极配合服从实习单位指导教师的管理。
> (3) 认真如实地在校友邦 APP 上撰写 10 天的实习日志和 2 次周志。对于反馈不合格的日志,要及时修改和补交。
> (4) 集中生产实习的指导工作,由(校内)实习责任教师和(校外)实习单位指导教师共同完成。遇到关于系统和技术方面的问题,请在工作时间联系实习单位指导老师。关于校友邦的日志和周志以及实习总结报告相关的问题,请在工作时间联系自己的实习责任老师。
